packages:
  - packages/*

# pnpm >=10 blocks dependency install scripts unless named here. Keep this list
# short and justified: every entry is third-party code we let run at install.
allowBuilds:
  esbuild: true # vitest's bundler; links its platform binary at install time
